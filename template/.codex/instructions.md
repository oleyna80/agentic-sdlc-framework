# Codex Instructions

Use the generated project `AGENTS.md` as the primary contract. Read the active
Work Block, relevant accepted specification, current branch/status, and only the
governance or workflow material needed for the requested stage.

Follow Define → Execute → Assure → Close. Keep one write-capable Coder within an
approved write-set; keep Critic, Reviewer, and Verifier read-only unless an
explicit documentation-evidence exception applies. Preserve unrelated
working-tree changes.

Use shared skills as procedures, not authority. Follow
`governance/decision-provenance.md` for material rationale. Do not stage,
commit, push, publish, deploy, or widen scope unless the active Work Block and
Owner authority permit it.
